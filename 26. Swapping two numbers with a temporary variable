#include <stdio.h>

int main() {
    int a, b, temp;
    printf("Enter two numbers: ");
    scanf("%d %d", &a, &b);
    printf("Original values of a and b: %d %d\n", a, b);
    temp = a;
    a = b;
    b = temp;
    printf("Swapped values of a and b: %d %d\n", a, b);
    return 0;
}
